// Pi shows package extension provenance based on the manifest entrypoint path.
// Keep the implementation in src while exposing a stable, user-facing package path
// instead of identifying the installed extension as "src".
export { default } from './src'
